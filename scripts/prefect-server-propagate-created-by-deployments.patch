--- deployments.py.orig
+++ deployments.py.patched
@@ -975,6 +975,17 @@
         if not flow_run.state:
             flow_run.state = schemas.states.Scheduled()

+        if flow_run.created_by is None and flow_run.parent_task_run_id:
+            parent_task = await models.task_runs.read_task_run(
+                session=session, task_run_id=flow_run.parent_task_run_id
+            )
+            if parent_task and parent_task.flow_run_id:
+                parent_run = await models.flow_runs.read_flow_run(
+                    session=session, flow_run_id=parent_task.flow_run_id
+                )
+                if parent_run and parent_run.created_by:
+                    flow_run.created_by = parent_run.created_by
+
         right_now = now("UTC")
         model = await models.flow_runs.create_flow_run(
             session=session, flow_run=flow_run
